<h2>商品登録</h2>

<div>
  <%= form_with model: @item, url: shop_items_path, method: :post, local: true do |f| %>
    <%= render "item_tp", f: f %>
    <div class="form-group">
      <%= f.submit "新規登録", class: "btn btn-outline-secondary btn-sm" %>
    </div>
  <% end %>
</div>
