<h2>会員情報編集</h2>

<%= form_with model: @user, url: users_path, method: :patch, local: true do |f| %>

  <%= render "edituser", f: f %>

  <div class="actions">
    <%= f.submit "変更を保存", class: "btn btn-outline-secondary btn-sm" %>
  </div>
<% end %>
